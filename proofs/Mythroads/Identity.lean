namespace Mythroads

/-- The stable subject identifier supplied by the authentication provider. -/
abbrev AuthId := String

/-- The persistent identifier of a player record. -/
abbrev PlayerId := String

/-- The stable catalogue or inventory identifier of an item. -/
abbrev ItemId := String

end Mythroads
